return {
  python = {
    analysis = {
      typeCheckingMode = "basic",   -- "off" | "basic" | "strict" — basic gives useful warnings without being noisy
      autoSearchPaths  = true,
      useLibraryCodeForTypes = true,
    },
  },
}
